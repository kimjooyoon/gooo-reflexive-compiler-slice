# gooo-reflexive-compiler-slice

Placeholder for the released-input experiment that will demonstrate one
Gooo-owned compiler phase.

The repository starts with a deliberately small `main` bootstrap. The feature
branch will add the compiler phase, its independent verifier, CI evidence, and
the immutable `v0.1.0` release.
